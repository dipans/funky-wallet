apiVersion: apps/v1
kind: Deployment
metadata:
  name: solana-chain-adapter
  namespace: funky-wallet
  labels:
    app.kubernetes.io/name: solana-chain-adapter
    app.kubernetes.io/part-of: funky-wallet
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: solana-chain-adapter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: solana-chain-adapter
        app.kubernetes.io/part-of: funky-wallet
    spec:
      containers:
        - name: solana-chain-adapter
          image: ghcr.io/dipans/solana-chain-adapter:latest
          ports:
            - containerPort: 9091
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 9091
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 9091
            initialDelaySeconds: 20
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: solana-chain-adapter
  namespace: funky-wallet
  labels:
    app.kubernetes.io/name: solana-chain-adapter
    app.kubernetes.io/part-of: funky-wallet
spec:
  selector:
    app.kubernetes.io/name: solana-chain-adapter
  ports:
    - port: 9091
      targetPort: 9091
