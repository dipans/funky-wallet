# IMPORTANT: Replace placeholder values with real base64-encoded secrets.
# In production these are injected by CI/CD (GitHub Actions → kubectl apply).
# Never commit real credentials here.
apiVersion: v1
kind: Secret
metadata:
  name: funky-wallet-secrets
  namespace: funky-wallet
  labels:
    app.kubernetes.io/part-of: funky-wallet
type: Opaque
data:
  # echo -n 'funky' | base64
  postgres-username: ZnVua3k=
  # echo -n 'changeme' | base64  ← replace in CI
  postgres-password: Y2hhbmdlbWU=
  # echo -n 'your-auth0-domain.us.auth0.com' | base64  ← replace in CI
  auth0-domain: eW91ci1hdXRoMC1kb21haW4udXMuYXV0aDAuY29t
  # echo -n 'your-client-id' | base64  ← replace in CI
  auth0-client-id: eW91ci1jbGllbnQtaWQ=
  # echo -n 'https://api.funkywallet.io' | base64
  auth0-audience: aHR0cHM6Ly9hcGkuZnVua3l3YWxsZXQuaW8=
